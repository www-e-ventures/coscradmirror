/**
 * This type alias allows us to update our id format across the codebase in
 * one fell swoop. For example, we may some day decide to use a `number` instead
 * or perhaps a particular format leveraging a template literal type.
 */
export type EntityId = string;
