/**
 * **Caution**
 * This prefix is used in auto-generated databases for test. It should never be
 * included anywhere in a production database (or any database you wish to persist).
 * Databases whose names include this string anywhere in their name will be
 * automatically dropped during test cleanup.
 */
export const TEST_DATABASE_PREFIX = 'testonly';

export const TEST_DATABASE_NAME_LENGTH = 20;
